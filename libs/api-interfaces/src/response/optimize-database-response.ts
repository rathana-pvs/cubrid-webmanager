/**
 * Client response type for optimizing a database.
 * Returns empty object on success (CMS envelope removed).
 *
 * @category Client Responses
 * @since 1.0.0
 */
export type OptimizeDatabaseResponse = {};
